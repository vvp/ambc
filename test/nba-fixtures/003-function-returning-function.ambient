func[
  write_ (in_scope).(
    in_scope|
    write_ ().(
      read_ (func)|
      func[
        write_ (in_scope).(
          in_scope|
          write_ (x, y).(
            string[
              plus[
                l[x[]]|
                r[y[]]
              ]
            ]|
            read_ (string))
        )
      ]
    )
  )
]
