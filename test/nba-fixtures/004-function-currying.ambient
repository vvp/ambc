func[
  write_ (in_scope).(
    {in_scope}|
    write_ (x).(
      read_ (func)|
      func[
        write_ (in_scope).(
          {in_scope}|
          write_ (y).(
            string[
              plus[
                l[x[]]|
                r[y[]]
              ]
            ]|
            read_ (string))
        )
      ]
    )
  )
]

